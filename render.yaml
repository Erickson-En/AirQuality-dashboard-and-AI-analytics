services:
  - type: web
    name: airquality-backend
    runtime: node
    rootDir: Backend
    buildCommand: npm install
    startCommand: node Server.js
    envVars:
      - key: NODE_ENV
        value: production
